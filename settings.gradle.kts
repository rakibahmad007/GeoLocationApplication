pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "GeoLocation" // Adjust if your project name is different
include(":app") // Ensure your app module is included
